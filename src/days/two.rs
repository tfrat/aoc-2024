use std::fmt::Display;
use crate::days::Day;

#[derive(Default)]
pub struct DayTwo {}

impl Day for DayTwo {
    fn part_one(&self, _input: &str) -> Box<dyn Display> {
        Box::new("TODO")
    }

    fn part_two(&self, _input: &str) -> Box<dyn Display> {
        Box::new("TODO")
    }
}